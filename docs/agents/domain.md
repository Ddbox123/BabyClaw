# Domain Docs

This repository uses a single-context domain documentation layout.

Agents should look for project-level domain context in:

- `CONTEXT.md`
- `docs/adr/`

Use `CONTEXT.md` for current product, architecture, and vocabulary context. Use `docs/adr/` for decision records that explain why major design choices were made.
